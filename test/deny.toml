[graph]
targets = [
    { triple = "aarch64-apple-darwin" },
    { triple = "aarch64-unknown-linux-gnu" },
    { triple = "x86_64-apple-darwin" },
    { triple = "x86_64-unknown-linux-gnu" },
]

[advisories]
version = 2
yanked = "deny"
unmaintained = "workspace"
ignore = [
]

[bans]
multiple-versions = "deny"
skip = [
    # Do not add to this list without good reason! Duplicate dependencies slow
    # down compilation and bloat the binary.

    # async-stripe is holding this back
    #{ name = "event-listener", version = "2.5.3" },
    #{ name = "event-listener", version = "5.3.1" },
    ## async-stripe is holding this back
    #{ name = "fastrand", version = "1.9.0" },
    #{ name = "fastrand", version = "2.1.0" },
    ## Required by hashbrown
    #{ name = "foldhash", version = "0.1.5" },
    ## async-stripe is holding this back
    #{ name = "getrandom", version = "0.1.16" },
    #{ name = "getrandom", version = "0.2.15" },
    ## Hickory resolver uses this older version,  while sentry uses a newer one.
    ## There are newer versions of of the resolver that update this,
    ## but the newer releases had issues last time we tried.
    ## TODO re-evaluate newer hickory resolver releases.
    #{ name = "hostname", version = "0.3.1" },
    ## hickory-resolver has updated to 2.0.6 before everything else
    ## and we have to upgrade that due to a security advisory
    #{ name = "thiserror", version = "1.0.63" },
    #{ name = "thiserror", version = "2.0.6" },
    #{ name = "thiserror-impl", version = "1.0.63" },
    #{ name = "thiserror-impl", version = "2.0.6" },
    ## waiting on https://github.com/arcnmx/serde-value/pull/35
    #{ name = "ordered-float", version = "2.10.1" },
    #{ name = "ordered-float", version = "5.0.0" },
    ## async-stripe is holding these back
    #{ name = "rand", version = "0.7.3" },
    #{ name = "rand", version = "0.8.5" },
    #{ name = "rand_chacha", version = "0.2.2" },
    #{ name = "rand_chacha", version = "0.3.1" },
    #{ name = "rand_core", version = "0.5.1" },
    #{ name = "rand_core", version = "0.6.4" },
    ## async-stripe is holding this back
    #{ name = "serde_qs", version = "0.8.5" },
    #{ name = "serde_qs", version = "0.10.1" },
    ## async-stripe requires this older version
    #{ name = "uuid", version = "0.8.2" },
    #{ name = "uuid", version = "1.17.0" },

    ## duplicates from the mz repo
    #{ name = "base64", version = "0.13.1" },
    #{ name = "base64", version = "0.22.1" },
    #{ name = "bitflags", version = "1.3.2" },
    #{ name = "bitflags", version = "2.6.0" },
    #{ name = "darling_macro", version = "0.20.11" },
    #{ name = "darling_core", version = "0.20.11" },
    #{ name = "darling", version = "0.20.11" },
    #{ name = "h2", version = "0.3.26" },
    #{ name = "h2", version = "0.4.5" },
    #{ name = "hashbrown", version = "0.14.5" },
    #{ name = "hashbrown", version = "0.15.5" },
    #{ name = "hashbrown", version = "0.16.1" },
    #{ name = "http", version = "0.2.12" },
    #{ name = "http", version = "1.1.0" },
    #{ name = "http-body", version = "0.4.6" },
    #{ name = "http-body", version = "1.0.1" },
    #{ name = "hyper", version = "0.14.30" },
    #{ name = "hyper", version = "1.4.1" },
    #{ name = "hyper-timeout", version = "0.4.1" },
    #{ name = "hyper-timeout", version = "0.5.1" },
    #{ name = "hyper-tls", version = "0.5.0" },
    #{ name = "hyper-tls", version = "0.6.0" },
    #{ name = "itertools", version = "0.10.5" },
    #{ name = "itertools", version = "0.14.0" },
    #{ name = "linux-raw-sys", version = "0.4.15" },
    #{ name = "linux-raw-sys", version = "0.9.4" },
    #{ name = "memmap2", version = "0.5.4" },
    ## Used by pprof
    #{ name = "nix", version = "0.26.4" },
    #{ name = "phf", version = "0.11.2" },
    #{ name = "phf", version = "0.13.1" },
    #{ name = "phf_shared", version = "0.11.2" },
    #{ name = "phf_shared", version = "0.13.1" },
    #{ name = "phf_generator", version = "0.11.2" },
    #{ name = "phf_generator", version = "0.13.1" },
    #{ name = "phf_codegen", version = "0.11.3" },
    #{ name = "phf_codegen", version = "0.13.1" },
    #{ name = "socket2", version = "0.5.10" },
    #{ name = "socket2", version = "0.6.0" },
    #{ name = "rustix", version = "0.38.44" },
    #{ name = "rustix", version = "1.0.7" },
    #{ name = "syn", version = "1.0.109" },
    #{ name = "syn", version = "2.0.5" },
    ## aws-lc-rs
    #{ name = "untrusted", version = "0.7.1" },
]

# Use `tracing` instead.
[[bans.deny]]
name = "env_logger"

# Use `prost` or `protobuf-native` instead.
[[bans.deny]]
name = "protobuf"

# Use `sha1` instead, which the RustCrypto ecosystem recently took control of.
# `sha-1` is the older and now deprecated name.
[[bans.deny]]
name = "sha-1"

# Strum has suspect code quality and includes many unneeded features. Use
# more targeted enum macro crates, e.g. `enum-kinds`.
[[bans.deny]]
name = "strum"
[[bans.deny]]
name = "strum-macros"

# We prefer the system's native TLS or OpenSSL to Rustls, since they are more
# mature and more widely used.
[[bans.deny]]
name = "rustls"

# The `uncased` crate serves the same purpose as `unicase` and is more
# actively maintained.
[[bans.deny]]
name = "unicase"

# we don't want to pull in workspace-hack from the materialize repo, because
# it contains a ton of dependencies that we don't want, making our builds
# slower and flakier, and making our version bump process more complicated
[[bans.deny]]
name = "workspace-hack"

[licenses]
version = 2
allow = [
    "Apache-2.0",
    "BSD-2-Clause",
    "MIT",
    "Unicode-3.0",
]
private = { ignore = true }

[[licenses.clarify]]
name = "ring"
expression = "ISC"
license-files = [
    { path = "LICENSE", hash = 0xbd0eed23 }
]

[sources]
unknown-git = "deny"
unknown-registry = "deny"
# Do not allow non-MaterializeInc Git repositories here! Git repositories must
# be owned by the MaterializeInc organization so that maintainership is shared
# amongst Materialize employees and so that historical versions of Materialize
# remain buildable even if upstream Git repositories disappear. If you don't
# have permissions to create a fork in the MaterializeInc organization, ask in
# #eng-infra on Slack.
#allow-org = { github = ["MaterializeInc"] }
